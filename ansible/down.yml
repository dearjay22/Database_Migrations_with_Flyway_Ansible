---
- name: Assignment 4 - Tear down MySQL
  hosts: localhost
  connection: local

  tasks:
    - name: Stop and remove MySQL container
      ansible.builtin.shell: |
        docker rm -f a4-mysql || true
      changed_when: false
